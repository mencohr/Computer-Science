"""Volume of sphere with radius five units"""
r = 5                                   # radius
vol_sphere = (4 / 3) * 3.14 * (r**3)

print("Vol of sphere with", (str(r) + "-unit radius is"), ("%.2f" % vol_sphere), "units")