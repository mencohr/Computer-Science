"""Reads a string, counts the characters, and prints "odd" or "even"."""

string = str(input("Submit a string: "))
count = 0

for letter in string:
    count += 1

if count % 2 == 0:
    print("Even")
else:
    print("Odd")